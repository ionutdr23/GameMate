apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-service
  namespace: gamemate-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: social-service
  template:
    metadata:
      labels:
        app: social-service
    spec:
      containers:
        - name: social-service
          image: keyshr3kt/social-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: social-service-secrets
            - configMapRef:
                name: social-service-configmap
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: social-service
  namespace: gamemate-backend
  labels:
    app: social-service
    prometheus: scrape
spec:
  selector:
    app: social-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
